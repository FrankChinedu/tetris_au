
export interface initOptionInt {
  score: number,
  setScore: (score: number) => void
  setGameOver: (gameOver: boolean) => void
  setDrop: (drop: any ) => void
  setMountControl: (controlDoc: any ) => void
}
